name: heavyrent_customer
description: Heavy Equipment Rental - Customer App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.19.0
  url_launcher: ^6.3.0
  image_picker: ^1.1.2
  cupertino_icons: ^1.0.8
  shared_preferences: ^2.2.2
  smooth_page_indicator: ^1.1.0
  http: ^1.2.2
  # Firebase
  firebase_core: ^3.6.0
  firebase_auth: ^5.3.1
  firebase_storage: ^12.3.2
  # Google Maps
  google_maps_flutter: ^2.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
